(require 'llvm/face/group)

(defface llvm/face/atomic-ordering
  '((t :inherit 'font-lock-preprocessor-face :foreground "#D65C04"))
  "Face for atomic orderings."
  :group 'llvm/face/group)

(provide 'llvm/face/atomic-ordering)
