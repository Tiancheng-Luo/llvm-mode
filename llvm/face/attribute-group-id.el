(require 'llvm/face/group)

(defface llvm/face/attribute-group-id
  '((t :inherit 'font-lock-preprocessor-face :foreground "#DECF2C"))
  "Face for attributes group identifiers."
  :group 'llvm/face/group)

(provide 'llvm/face/attribute-group-id)
